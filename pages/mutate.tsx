import Layout from "../components/layout"
import Mutator from "../components/Mutator/mutator"

export default function MutatePage() {
    return (
        <Layout>
            <h1>GitHub API (GraphQL) Mutator</h1>
            <Mutator />
        </Layout>
    )
}
